# Copy to .env and fill in. On Render, DATABASE_URL is injected from the managed Postgres.
# Local dev: point at a local Postgres or a Render/Neon dev database.
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/cpq?schema=public"

# Port the Express server listens on (Render sets PORT automatically).
PORT=3001
